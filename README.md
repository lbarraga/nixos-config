```
.
├── flake.lock
├── flake.nix                          # Minimal entry point: imports modules/ recursively
└── modules/
    ├── nix/                           # Technical machinery
    │   └── flake-parts.nix            # Framework setup & mkNixos helper
    ├── core/                          # The functional foundation
    │   └── core.nix                   # Shared DNA: Locale, Timezone, Bootloader, sops
    ├── hosts/                         # The actual machines (the "collectors")
    │   ├── desktop/
    │   │   ├── desktop.nix            # Host definition: imports core, user, hardware, and apps
    │   │   ├── hardware-configuration.nix # Autogenerated NixOS hardware config (imported as local file)
    │   │   └── secrets.yaml           # Desktop-specific secrets
    │   └── laptop/
    │       ├── laptop.nix             # Host definition
    │       ├── hardware-configuration.nix 
    │       └── secrets.yaml           
    ├── hardware/                      # Physical capabilities & drivers
    │   ├── nvidia.nix                 # Standalone Nvidia feature (Simple Aspect)
    │   ├── bluetooth.nix
    │   ├── printing.nix
    │   └── sound.nix
    ├── connectivity/                  # Networking and remote access
    │   ├── pangolin.nix               
    │   ├── tailscale.nix              
    │   └── ssh.nix
    ├── desktop-environment/           # Graphical environments
    │   └── hyprland/                  # Multi-file feature folder
    │       ├── hyprland.nix           # Multi-Context Aspect: Links NixOS & HM
    │       ├── config.nix             # Window manager rules
    │       └── waybar/                # Scripts and CSS
    ├── editors/
    │   └── nvim.nix
    ├── apps/                          # Software features (Many are Multi-Context Aspects)
    │   ├── common-cli.nix             # eza, htop, tree
    │   ├── personal.nix               # Spotify, Discord, Obsidian
    │   ├── 3d-printing.nix            # Udev rules (NixOS) + OrcaSlicer (HM via sharedModules)
    │   ├── steam.nix
    │   └── git.nix                    # Git + Signing
    └── users/                         # User profiles
        └── lukasbt.nix                # Minimal account info; hands off to Home-Manager
```

